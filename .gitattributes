# Keep LF in the working copy on Windows too. `dart format --set-exit-if-changed`
# runs on Linux in CI and treats CRLF files as unformatted.
* text=auto eol=lf

*.pdf binary
*.png binary
*.jpg binary
*.jks binary
*.keystore binary
